#ifndef H_UTILS
#define H_UTILS
#include <math.h>
#include <vector>
#include <utility>
#include <string>
#include <chrono>

//declare some structs and typedefs that help make the code cleaner.
typedef std::milli time_ratio; // in what unit will we be measuring time.
extern const std::string time_ratio_name;
#endif